
<table class="table">
    <tr>
        <th>Name</th>
        <th>Action</th>
        <th></th>
        <th></th>
    </tr>

    @foreach (var item in Model.Items)
    {
        <tr>
            <td>@item.Name</td>
            <td><a asp-controller="@Model.Controller" asp-action="Edit" asp-route-id="@item.ID">Edit</a></td>
            <td></td>
            <td></td>
        </tr>
        
    }
</table> 